# Configuração de ambiente

Coloque um arquivo `.env` nesta pasta com as credenciais e URLs exigidas pela API JETTAX. O aplicativo Streamlit carrega automaticamente `config/.env` e também um `.env` na raiz do projeto.
